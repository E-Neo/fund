[workspace]

[package]
name = "fund-strategies"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
toml = "1.1.4"
wit-bindgen = "0.61.1"

[profile.release]
panic = "abort"
opt-level = "s"
